# ── SmartLine — variáveis de ambiente ────────────────────────
# Copie este arquivo para .env e ajuste os valores

# URL do banco de dados SQLite (padrão: arquivo local)
DATABASE_URL=sqlite:///./smartline.db

# Porta do servidor (padrão: 5000)
PORT=5000